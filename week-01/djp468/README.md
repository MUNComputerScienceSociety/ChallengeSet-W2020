Above Average, A Towering Problem, Autori, and Amsterdam Distance. 

In C

Because why not.


```
cd <name>
gcc -g -O2 -std=gnu11 -static main.c -lm
cat ../../test-samples/<name>/<test>.in | ./a.out
```